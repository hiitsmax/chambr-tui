# Contributing

## Setup

```bash
yarn install
yarn lint
yarn build
```

## Pull requests

- Open PRs against `main`.
- Keep changes scoped and include tests when relevant.
- Use clear commit messages.

## Versioning and release

Releases are manual and tag-based (`vX.Y.Z`). Consumers pin to tags in `package.json`.
